//import { JWT } from "jose";

export const jwtDecoded = (jwt) => {
  // const secret = import.meta.env.VITE_JWT_SECRET;
  // const decodedJwt = JWT.decode(jwt, {
  //   secret: secret,
  // });
  // return decodedJwt;
};
